trigger:
- master

pr: none

variables:
  Major: 1
  Minor: 0
  DotNetSdkVersion: 3.0.100

stages:
  - stage: Test
    jobs:
      - job:
        steps:
          - script: |
              echo TestStage
              echo $(Major)
              echo $(Minor)
              echo $(DotNetSdkVersion)

  - stage: Build
    jobs:
      - job:
        steps:
          - script: echo BuildStage

  - stage: Staging
    jobs:
      - job:
        steps:
          - script: echo Staging

  - stage: Production
    jobs:
      - job:
        steps:
          - script: echo Production